{% extends 'base.html.twig' %}

{% block title %}Livro{% endblock %}

{% block content %}
    <h1>Livro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livro.id }}</td>
            </tr>
            <tr>
                <th>Genero</th>
                <td>{{ livro.genero }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ livro.titulo }}</td>
            </tr>
            <tr>
                <th>Editora</th>
                <td>{{ livro.editora }}</td>
            </tr>
            <tr>
                <th>Autor</th>
                <td>{{ livro.autor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('livro_index') }}">back to list</a>

    <a href="{{ path('livro_edit', {'id': livro.id}) }}">edit</a>

    {{ include('livro/_delete_form.html.twig') }}
{% endblock %}
